<Page x:Class="SubtitleEditor.View.SubEventPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:SubtitleEditor.View"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      xmlns:vm="using:SubtitleEditor.ViewModel"
      NavigationCacheMode="Enabled">
    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="sample"
                   Text="Sample 示例 サンプル　さんぷる" 
                   FontFamily="{Binding ElementName=fontfamily, Path=SelectedItem}"/>
        <ComboBox x:Name="fontfamily">
            <ComboBox.ItemTemplate>
                <DataTemplate x:DataType="FontFamily">
                    <TextBlock Text="{x:Bind Source}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Button Content="set"
                Click="Button_Click" />
    </StackPanel>
</Page>
